import { Shell } from "@/components/Shell";

export default function SettingsPage() { return <Shell><header className="topbar"><div><span className="eyebrow">Workspace / settings</span><h1>Keep it grounded.</h1><p className="subtitle">Configuration that is safe to expose before backend contracts are complete.</p></div><div className="profile"><div className="avatar">NS</div></div></header><div className="card"><div className="card-heading"><h2>API connection</h2><span className="pill warm">Environment only</span></div><div className="form-grid"><div className="field"><label>API base URL</label><input value={process.env.NEXT_PUBLIC_API_URL ?? "Not configured"} readOnly /></div><div className="field"><label>Content persistence</label><select defaultValue="draft"><option value="draft">Local draft mode</option><option value="api">API mode (when contract is ready)</option></select></div></div><div className="notice" style={{ marginTop: 22, marginBottom: 0 }}>Do not place JWTs, provider secrets or storage credentials in this client. Authentication and CMS write endpoints must be added through the API boundary.</div></div></Shell>; }
